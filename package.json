{
  "name": "topsecret-env-example",
  "version": "0.1.0",
  "description": "example usage of topsecret-env package",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.js",
    "generate": "topsecret generate --key-file _secret.key",
    "encrypt": "sh _server_and_tenants.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mike Terry",
  "license": "ISC",
  "dependencies": {
    "topsecret-env": "^0.1.4"
  }
}
